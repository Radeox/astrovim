# Astrovim

My configuration for astrovim.

## Install

```bash
mv ~/.config/nvim ~/.config/nvim.bck
cp -r nvim ~/.config/
```

### Note for future me

The `init.lua` is managed by NixOS so remove it before copying the configuration
